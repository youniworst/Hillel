/* 'use strict';

let userName = prompt('Enter test');

switch (userName) {
    case 'test':
        alert('Its test')
        break;
    case 'Oleg':
        alert('Its Oleg')
        break;
    default:
        alert('Its not test and its not Oleg')
    break;
}
 */

let str = 'test'

for(let i = 0; i < str.length; i++) {
    console.log(str[i]);
}